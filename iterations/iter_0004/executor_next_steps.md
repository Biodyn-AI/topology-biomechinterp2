# Executor Next Steps — iter_0004

1. Run stronger-null stress tests (distance permutation + kNN rewiring) on top layers in immune and external-lung to verify H03 is not null-family specific.
2. Add split-regime robustness for topology (source-disjoint and target-disjoint gene partitions) using the same H1 protocol.
3. Re-run intrinsic-dimensionality coupling with a more stable local-ID estimator and include prior lung results in one joint meta-analysis.
4. Add biological anchoring by testing whether high-persistence neighborhoods/layers are enriched for TRRUST regulator-target modules.
