# Executor Next Steps — iter_0018

1. Retire the current `H37` tier-gap formulation and only rescue with a materially changed design: prevalence-balanced tier construction plus full multiseed target-disjoint replication.
2. Escalate `H38` with strict generalization testing (leave-layer-out / held-out split fits) to verify whether large in-sample `ΔR²` survives out-of-sample controls.
3. Continue `H39` as a topology-stability branch with stronger null sensitivity (more feature-shuffle permutations and bootstrap-resampled point clouds) before claiming robust PH excess.
4. Keep cross-model alignment deprioritized this loop unless new anchor-causality controls provide a materially different method from the repeated inconclusive branch.
