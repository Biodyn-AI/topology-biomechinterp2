# Executor Next Steps (iter_0040)

1. Retire tested forms immediately:
   - `H103` interaction-only derivative rescue (`N508` tested form),
   - `H104` depth motif grammar additive screen (`N520` tested form),
   - `H105` conditioned-null rescue expectation (`N519` tested form).

2. Next iteration packet should shift to non-retired/high-upside space:
   - one `module_structure` hypothesis with hierarchical local pooling (GO/Cell Ontology, not flat pooled overlays),
   - one `manifold_distance` hypothesis focused on geodesic corridor/path multiplicity with strict rewiring null,
   - one `split_robustness` confirmatory stress test on already-promising anchors (`H91/H93`) with reduced method novelty but higher null resolution.

3. Hard gates for the next packet:
   - require positive mean null-gap in at least half of domain-splits for promotion,
   - stop any branch early if it yields `0` positive null-gap domain-splits after pilot null budget,
   - keep at most one carry-over refinement slot.
