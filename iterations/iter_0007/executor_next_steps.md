# Executor Next Steps — iter_0007

1. Run external-lung full-layer replication of the same metric-matched rewiring protocol to test whether the uniformly negative calibrated result is immune-specific or cross-domain.
2. Add a stricter calibration control: degree-preserving rewiring constrained to preserve edge-length quantile bins, then retest geodesic H1 deltas and Fisher p-values.
3. Prioritize biological anchoring on the still-positive feature-shuffle late layers (`7,9,10,11` from `iter_0006`) with TRRUST/GO enrichment to avoid overfocusing on non-supportive rewiring branches.
4. Add split-regime extension to `dual_axis_disjoint` for a small subset of late layers to test whether any residual positive signal survives stronger disjointness.
