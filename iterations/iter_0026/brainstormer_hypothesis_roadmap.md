# Brainstormer Hypothesis Roadmap - iter_0026

## Retire / Deprioritize
| Direction | Status | Why | Re-entry condition |
|---|---|---|---|
| Undirected curvature-assortativity direct scoring (`H61`-type) | `retire_now` | Net-negative aggregate and weak cross-domain portability. | Only as interaction with stronger directed/topological features. |
| Standalone intrinsic-dimension AUROC-lift (`H60/H63` forms) | `retire_now` | Endpoint and transition variants both negative. | Only as interaction/modulator, not primary endpoint. |
| Directed/signed weighting rescues (`H58`-type) | `retire_now` | No measurable rescue over unweighted baseline. | Only if topology representation itself changes. |
| Path-homology utility-transfer endpoint (`H53/H56`-type) | `retire_now` | Utility transfer gate repeatedly failed. | New endpoint and new transfer target required. |
| Rewiring-survival null branch (`H05-H12`) | `retire_now` | Extensive calibration attempts remained non-supportive. | Re-open only if paired with new biological endpoint. |
| Anisotropy-tail standalone endpoint (`H57`-type) | `retire_now` | Broad-screen keep gate failed. | Re-open only as feature interaction term. |
| Cross-model alignment (raw/anchor Procrustes forms: `H59/H62`) | `rescue_once_with_major_change` | Directional gains exist but null-gap remains negative outside immune. | One final cycle-consistent/null-gap-first redesign. |
| Bifiltration utility-coupling claim (`H49` endpoint form) | `rescue_once_with_major_change` | Discrimination robust, utility claim unstable. | One redesign with calibration ranking endpoint. |

## New Hypothesis Portfolio
| ID | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|
| N315 | Two-parameter persistence over `(distance, directed-support margin)` will recover source-disjoint failure slices missed by scalar topology scores. | Build bifiltration on existing directed/signed graph at layers `{7,11}` then `{0,3,7,11}`; add persistence-image features to `H55` baseline and test incremental AUROC/calibration across all domains/seeds/splits. | `lung/source_disjoint` and `external_lung/source_disjoint` mean deltas move to non-negative, with `>=4/6` domain-splits positive. | Margin permutation within degree bins, support-score permutation, one-parameter ablation. | high | high |
| N316 | Split-vineyard stability (birth/death trajectory smoothness across resamples) is more predictive than raw persistence magnitude. | Bootstrap node/edge subsamples per split/layer, compute vineyard trajectory variance and recurrence features, then test incremental lift over `H55`/`H47` features. | Low-variance/high-recurrence topological features enrich positives and improve calibration slope/ECE. | Resample-label permutation and random trajectory pairing controls. | medium | medium |
| N317 | Relative homology around TF ego-neighborhoods captures regulatory closure deficits linked to true edges. | For each candidate edge, compute relative Betti/lifetime features between TF-local complex and matched background complex; run nested model vs distance+directed baseline. | Positive nested-model gain in at least two domains, strongest in source-disjoint slices. | TF label shuffle preserving degree and neighborhood-size matched random ego-complexes. | medium | medium |
| N318 | Signed directed motif persistence (feed-forward/feedback motif complexes) encodes mechanistic signal beyond pairwise topology. | Build 3-node signed directed motif complexes, compute motif persistence and motif-transition persistence, add to baseline scorer. | Positive coefficients for specific motifs and reproducible AUROC lift in `>=2/3` domains. | Direction/sign shuffle preserving in/out degree and sign marginals. | medium | medium |
| N319 | Stability-corrected topological entropy (persistent entropy + landscape roughness) outperforms raw lifetime deltas. | Compute entropy/roughness per edge-neighborhood diagram with bootstrap CIs; compare to raw lifetime features on same evaluation matrix. | Better calibration and positive mean delta in failure slices without harming immune target performance. | Filtration-order randomization and bootstrap label permutation. | medium | low |
| N320 | Sheaf-cohomology inconsistency on signed directed graphs identifies false edges that classical PH misses. | Build sheaf Laplacian with sign/orientation constraints, compute harmonic residual/cohomology energy around edges, and test incremental utility. | Lower inconsistency for positives and measurable nested-model gain across domains. | Random sign sheaf, incidence permutation preserving degree, label permutation. | high | high |
| N321 | Curvature dynamics across layers (slope/acceleration), not static curvature, carry regulatory signal. | Compute per-layer curvature (Forman/Ollivier proxy), derive transition slope/acceleration features, and interact with directed margins. | Positive interaction terms and source-disjoint uplift where static curvature failed. | Layer-order permutation and curvature-within-degree shuffles. | medium | low |
| N322 | Tangent transport alignment between regulator and target neighborhoods reveals causal manifold coupling. | Fit local tangent bases (PCA) per layer, estimate transition transport maps, score edge transport-angle consistency, test against geodesic baseline. | Positives show lower transport mismatch and improved AUROC in late layers. | Neighborhood permutation preserving local degree/density and label permutation. | medium | medium |
| N323 | Multiscale geodesic triangle-defect spectra detect structurally constrained regulatory pairs. | For each edge, sample third-node triangles at scales `k={8,12,16}`, compute defect quantiles/skew, and add to baseline model. | Low-defect-tail enrichment for positives and robust gains in at least four domain-splits. | Endpoint swap within geodesic bins and random third-node matching. | medium | low |
| N324 | Intrinsic dimension is useful only as an interaction term with directed support, not as a standalone score. | Compute local ID variance/gradient features and include only interactions with directed/signed margins/support scores; run full multiseed screen. | Interaction coefficients positive with non-negative global delta even if ID main effect is near zero. | Interaction-partner shuffle, layer-order permutation, label permutation. | medium | low |
| N325 | Immune-first anchor-constrained cycle-consistent graph matching can convert directional cross-model gains into null-robust transfer. | Learn bidirectional scGPT↔Geneformer maps with anchor supervision and cycle loss on immune first; expand to lung/external-lung only if immune passes null-gap. | Immune `null_gap_q95>0`, then at least one non-immune split with positive null-gap and significant Fisher aggregate. | Random-map alignment, anchor-label shuffle, signature-destroy permutations. | high | medium |
| N326 | Cross-model topology codebook transport (OT over discrete topological tokens) will outperform continuous Procrustes alignment under strict nulls. | Cluster local topology signatures into tokens per model, learn OT transport on shared genes, score edges via token compatibility, evaluate null-gap robustness. | Larger null-gap than `H62` and positive transfer in lung/external-lung where Procrustes failed. | Random codebook mapping, token-frequency matched shuffle, domain-pair shuffle. | high | high |
| N327 | Cell-ontology and GO proximity as a second filtration axis will isolate biologically plausible topology signal. | Add ontology/program proximity to filtration (distance x ontology axis), evaluate incremental gain and stratum-specific calibration in failure slices. | Stronger gains in ontology-proximal strata and improved source-disjoint behavior. | Ontology label shuffle and size-matched random program assignment. | high | medium |
| N328 | Mechanistic motif persistence classes (TRRUST/STRING-informed) define reusable representation-space signatures of regulation. | Build class-conditional motif persistence signatures and score edges by class-compatible signatures across layers/splits. | Significant class-specific enrichment and incremental predictive lift over class-agnostic baseline. | Mechanism-class permutation within degree bins and label permutation. | medium | medium |

## Top 3 for Immediate Execution
| Role | Candidate | Why now | Minimal success gate |
|---|---|---|---|
| High-probability discovery candidate | N315 | Directly targets current source-disjoint failures while staying attached to the strongest surviving topology branch. | Both source-disjoint failure slices become non-negative and `>=4/6` domain-splits have positive mean incremental AUROC. |
| High-risk/high-reward candidate | N326 | Biggest chance to break current cross-model null-gap ceiling with a materially different transfer representation. | Mean `null_gap_q95 > 0` in immune and positive null-gap in at least one of lung/external-lung. |
| Cheap broad-screen candidate | N324 | Low-cost reuse of existing ID pipeline with a new interaction-only objective that avoids retired standalone ID endpoints. | Non-negative global mean incremental AUROC and at least one Fisher-significant domain-split aggregate. |
