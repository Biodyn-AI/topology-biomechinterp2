# Executor Next Steps — iter_0026

1. Prioritize one non-retired, high-upside topology packet outside current negative branches: test directed module-structure or signed community-surrogate features with explicit source-disjoint failure-slice diagnostics.
2. If cross-model is kept for one final rescue, constrain to immune-only and switch to a stricter anchor objective (pairwise TF-target contrastive loss with explicit null-gap optimization); otherwise retire cross-model for this loop.
3. Retire standalone intrinsic-dimensionality scoring variants (`endpoint` and `transition` forms) unless a materially different endpoint is introduced (e.g., perturbation-grounded calibration rather than AUROC lift).
