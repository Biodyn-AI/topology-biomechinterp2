# Executor Next Steps - iter_0033

1. Promote `H82` to a confirmatory pass with stricter controls: rerun with a second hotspot definition (top-decile vs top-quintile), add explicit endpoint-swap null for local-cycle gap, and require preservation of `>=4/6` positive null-gap domain-splits.
2. Retire the current `H84` shortcut-bridge additive formulation (`0/6` positive mean deltas and `0/6` positive mean null-gaps); only reopen if SBC is used as a conditional modifier inside the validated H70/H82 geometry branch.
3. Keep `H83` retired for utility claims after this invariance pilot failure (`0/3` positive null-gap domains); reopen only with a materially different cross-model anchor (for example ontology-stratified pathway subsets with explicit domain matching constraints).
4. Use one slot next iteration for `H82` confirmation and two slots for orthogonal novelty families (`null_sensitivity` and `module_structure` with localized, depth-conditional objectives) to maintain breadth.
