# Next Steps — iter_0021

1. Promote `H47` to multi-seed validation.
   - Run the same bifiltration packet for seeds `42/43/44` and test sign stability of `ΔAUROC(bif - distance-only)` by domain/split.

2. Run one focused rescue for `H48` (immune-only).
   - Expand motif family (`FFL`, bifan, feedback triads), increase null budget, and test split-regime robustness (`source_disjoint` vs `target_disjoint`) before any cross-domain claim.

3. Retire current `H46` weighting formulation unless materially changed.
   - If rescued, replace thresholded weighted zigzag with edge-level weighted persistence features and evaluate against an explicit held-out utility target.
