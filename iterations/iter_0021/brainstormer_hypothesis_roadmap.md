# Brainstormer Hypothesis Roadmap - iter_0021

## Retire / Deprioritize

| Direction | Status | Why now | Allowed future action |
|---|---|---|---|
| Rewiring-survival null lineage (`H07`, `H09`, `H11`, `H12`) | `retire_now` | Multiple consecutive negatives plus confounded bridge diagnostics; no utility-bearing rescue signal. | Do not schedule in next loop. |
| GW-first correspondence recovery (`H27`, `H29`) | `retire_now` | Repeated failure on both mapping quality and transfer utility. | Keep retired unless external supervised anchors change the setup. |
| Raw curvature/thinness direct scoring (`H23`, `H30`) | `retire_now` | Consistent underperformance versus geodesic/diffusion baselines. | Replace only with materially richer geometry features. |
| Coarse confidence/tier concentration variants (`H19`, `H37`) | `retire_now` | Directionally failed across iterations. | Only return via continuous support formulations. |
| Weighted-threshold zigzag coupling (`H46`) | `retire_now` | Main objective failed on full packet (`0/36` weighted wins). | Re-enter only with non-threshold weighted persistence method. |
| OOS ID delta-R2 branch (`H42`, `H45`) | `retire_now` (current form) | Instability and contradictory robust metrics. | Re-enter only with different target and larger holdout blocks. |
| Cross-model motif overlap (`H48`) | `rescue_once_with_major_change` | Immune-only signal with sparse/degenerate null regime in other domains. | One rescue with anti-sparsity motif design, then retire if still domain-narrow. |
| Cross-model alignment objective (`H33`, `H36`) | `rescue_once_with_major_change` | Structural alignment exists but utility attribution is weak. | One utility-first/topology-first rescue, then retire if null remains non-discriminative. |

## New Hypothesis Portfolio

| ID | Theme | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N212 | Persistent homology exploitation | Bifiltration gain from `H47` is seed-stable and predicts downstream utility variation across domains. | Expand `H47` to seeds `42/43/44`; compute per-row `delta_AUROC(bif-distance)` and test coupling to utility anchors (`H31/H43`) with leave-domain-out checks. | Positive deltas in most rows plus positive utility coupling in at least `2/3` domains. | Support shuffle within distance strata, distance-only ablation, and layer-order permutation placebo. | high | medium |
| N213 | Directed/signed topology | Directed/signed persistence captures regulatory asymmetry missed by undirected cycle-rank summaries. | Build directed kNN edge complexes oriented by TF-prior direction/support asymmetry and compute directed cycle/path-homology features against undirected baseline. | Incremental AUROC/log-loss gain over undirected in at least `2` domains. | Degree-preserving random edge orientation and sign-shuffle controls. | high | high |
| N214 | Topology dynamics | Layer-to-layer barcode-vineyard dynamics (velocity/acceleration of bars) explain where utility peaks emerge. | Match bars across adjacent layers and derive vineyard drift statistics; model utility as function of drift features. | Stable directional coupling (e.g., larger late-layer drift predicts higher utility) with split robustness. | Layer-order shuffle and random bar-matching controls. | medium | medium |
| N215 | Multiparameter filtration | A tri-filtration surface (`distance x STRING x GO`) yields stronger topology-to-label signal than current two-parameter support filtering. | Compute grid summaries (rank invariant/Hilbert summaries) and compare to `H47` on identical edge-label tasks. | Significant incremental gain in Fisher-combined tests across domain-splits. | Distance-only and distance+shuffled-biology ablations with matched degree strata. | high | high |
| N216 | Curvature rescue | Curvature only becomes predictive when conditioned on biological support and geodesic context. | Compute Ollivier/Forman features, add interaction terms with support deciles and geodesic detour features in nested models. | Positive interaction coefficients and AUROC improvement vs geometry-only baseline. | Rewired graph controls preserving degree/edge-length plus support shuffles. | medium | medium |
| N217 | Geodesic tails | Tail geodesic defect statistics (not means) carry the strongest regulatory signal. | Extract 90th/95th quantile triangle-defect and detour-tail features around edge endpoints; compare to existing `H32` feature set. | Tail features produce positive incremental value in multiple domain-splits. | Local neighborhood bootstrap and distance-matched random-edge controls. | medium | low |
| N218 | Local linearity mechanism | Local tangent-subspace drift across depth is a robust mechanistic predictor of positive regulatory edges. | Compute local PCA bases per layer and principal-angle drift per gene/edge; evaluate with disjoint splits and multi-seed aggregation. | Positive and split-stable predictive lift with significant coefficients for drift features. | Neighborhood reassignment within degree bins and label permutation. | high | medium |
| N219 | Intrinsic-dimension reformulation | Cross-estimator ID disagreement (uncertainty) predicts failure/success regions better than absolute ID values. | Build features from TWO-NN/MLE/participation-ratio disagreement and evaluate OOS rank/correlation targets instead of delta-R2. | Consistent positive OOS rank correlation and improved calibration reliability. | Estimator-label shuffle, layer permutation, and random-breakpoint controls. | medium | low |
| N220 | Cross-model topology transfer | Persistence-image signatures align across scGPT and Geneformer even when gene-level correspondences are weak. | Compute persistence images per domain/layer/model on matched gene sets; test CKA/Spearman similarity and utility transfer of aligned signatures. | Significant cross-model topology similarity in at least `2` domains and transfer above random maps. | Random gene-map permutations and within-model layer shuffles. | high | medium |
| N221 | Module-level alignment | Module-centric cross-model alignment is more stable and useful than gene-centric alignment. | Aggregate embeddings into TRRUST/GO modules, align module centroids, then evaluate transferred module-edge priors on held-out domains. | Module-level transfer utility exceeds gene-level baseline with significant null separation. | Size-matched module-membership shuffle and random centroid mapping. | high | medium |
| N222 | Biological anchoring | Top persistent generators are enriched for TRRUST/GO/STRING programs tied to domain cell identity. | Extract genes participating in top persistence generators from `H44/H47` and run matched enrichment vs degree/expression-matched random sets. | Reproducible enrichment (q-value pass) and coherent domain-specific biology. | Matched random generator sets and null-complex generators. | high | medium |
| N223 | Cell ontology geometry | Cell-ontology distance along manifold geodesics mediates support-to-utility effects. | Build ontology-gradient coordinates, compute geodesic trajectory features, and run mediation models with domain/layer fixed effects. | Significant mediated path with reduced direct support term across domains. | Ontology label permutation and placebo gradient axes. | medium | high |
| N224 | Mechanistic motif fingerprints | Expanded motif fingerprints across models become cross-domain once sparsity is reduced by adaptive-k and module-collapsed edges. | Extend motif panel (FFL, bifan, feedback triad, feedforward chain, multi-input motifs) with `k={100,200,400}` and module-collapsed edge variants. | Significant overlap enrichment in at least `2` domains; null variance non-degenerate in most rows. | Degree-preserving rewiring and module-membership shuffle. | medium | low |
| N225 | Algorithmic signatures | Topological hotspots exhibit low commutator drift in local linear operators, indicating reusable computation circuits. | Fit local linear maps across adjacent layers on hotspot gene sets and test commutator norms as edge-level predictors. | Lower commutator norm associates with higher positive-edge probability and improved calibration. | Matched random hotspot sets and edge-label permutation. | high | high |

## Top 3 for Immediate Execution

| Role | Candidate | Why this now | Minimal success gate |
|---|---|---|---|
| High-probability discovery | `N212` | Directly extends the strongest current positive (`H47`) with the missing robustness and utility-link checks. | `delta_AUROC(bif-distance)` positive in `>=70%` rows, Fisher-significant in `>=4/6` domain-splits, and positive utility coupling in `>=2/3` domains. |
| High-risk / high-reward | `N213` | Opens a genuinely new topology axis (directed/signed persistence) not yet tested in this loop family. | Directed/signed features beat undirected baseline in `>=2` domains under orientation-preserving nulls. |
| Cheap broad-screen | `N224` | Fast to implement using existing `H48` machinery while fixing the known sparsity failure mode. | Significant motif enrichment in `>=2` domains with non-zero null variance in `>=80%` rows. |
