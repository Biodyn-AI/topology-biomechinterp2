# Executor Next Steps — iter_0029

1. Promote `H70` to a targeted confirmatory pass with stronger biological controls: add coexpression-matched and support-shuffle baselines, then test whether the matched-random-third null-gap remains positive in immune splits where effect size is weakest.
2. Keep the current `cross_model_alignment` transfer endpoint retired after `H71`; do not spend next-iteration budget on minor map/objective tweaks in this family.
3. If a cheap breadth slot is available, run one tightened `H72` follow-up (`>=48` null permutations and multiseed expansion) and retire immediately if null-surviving utility remains non-positive.
