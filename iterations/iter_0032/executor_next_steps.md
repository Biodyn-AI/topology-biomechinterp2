# Executor Next Steps - iter_0032

1. Retire `H81` detour-elasticity utility endpoint in its current form; do not allocate primary-slot budget without a materially different perturbation target (e.g., edge-local node ablation with explicit path witness tracking).
2. Move `H80` to hold/retire status for utility claims; if revisited, require a stronger invariant endpoint (e.g., pathway trajectory stability across depth with pre-registered null-gap gate) and avoid another global correspondence objective.
3. Keep `H79` as a narrow follow-up candidate only if changed to depth-conditional module calibration (layer-11-focused) with stricter module-size controls and an a priori requirement of `>=2/6` positive domain-split null gaps.
4. Increase breadth next iteration by rotating to a non-retired family (`graph_topology` or `null_sensitivity`) with one cheap orthogonal probe plus one targeted `H70`-lineage check.
