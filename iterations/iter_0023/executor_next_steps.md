# Next Steps — iter_0023

1. Promote `H52` with higher-resolution null calibration and error analysis.
   - Re-run `H52` with `>=24` permutations/null family (to remove the current permutation p-value floor) and add a focused diagnostic for `lung/source_disjoint` where mean delta is negative.

2. Park `H53` unless objective is materially changed.
   - Do not spend another loop on this exact pilot; only reopen with a changed endpoint (utility transfer or cross-model topology transfer) and denser directed-complex construction.

3. Retire `H54` rupture-index branch.
   - The current intrinsic-geometry formulation is decisively negative (`mean delta AUROC=-0.04527`), so future intrinsic work should use a different signal family rather than rupture-over-reconstruction trajectories.
