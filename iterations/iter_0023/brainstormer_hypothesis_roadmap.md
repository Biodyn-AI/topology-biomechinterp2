# Brainstormer Hypothesis Roadmap - iter_0023

## Retire / Deprioritize

| Direction | Status | Why now | Re-entry condition |
|---|---|---|---|
| Local-linearity rupture endpoint (`H54`) | `retire_now` | Large negative effect (`mean delta_AUROC=-0.04527`) and broad cross-domain failure. | Do not re-run this formulation. |
| Rewiring-survival lineage (`H05-H12`) | `retire_now` | Repeated calibrated negatives under multiple stronger-null families. | Re-open only with fundamentally new biological objective. |
| GW-first correspondence recovery (`H27`, `H29`) | `retire_now` | Repeated failure on mapping quality and transfer utility. | Re-open only if new external correspondence supervision appears. |
| Raw curvature/thinness direct scores (`H23`, `H30`) | `retire_now` | Repeated underperformance vs geodesic/diffusion baselines. | Only re-open as interaction features, not standalone predictors. |
| Weighted-zigzag coupling objective (`H46`) | `retire_now` | Objective failed outright (`0/36` weighted wins). | Re-open only with non-threshold weighting + new target. |
| Motif-overlap-as-endpoint (`H48`, `H51`) | `retire_now` (current objective) | Descriptive enrichment does not survive strict module-level controls. | Re-open only as utility/topology transfer objective. |
| Current OOS ID `delta_R2` metric (`H42`, `H45`) | `retire_now` (current metric) | Contradictory and unstable holdout behavior. | Re-open with a different target metric and calibration test. |
| Directed path-homology pilot (`H53` exact form) | `rescue_once_with_major_change` | Weak directional signal without null-robust support (`0/6` Fisher-significant groups). | One redesign with denser complexes and utility-first endpoint. |
| Bifiltration utility-coupling claim (`H49`) | `rescue_once_with_major_change` | Discrimination is stable; utility coupling remains weak/placebo-sensitive. | One held-out utility redesign with strict placebo gate. |
| Curvature family overall | `rescue_once_with_major_change` | Standalone variants failed; interaction variants remain untested. | One interaction-only rescue with nested-model significance gate. |

## New Hypothesis Portfolio

| ID | Theme | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N241 | Topology stability | Directed/signed gains persist under higher null resolution and the `lung/source_disjoint` failure is a slice-specific graph-regime issue, not a global collapse. | Re-run `H52` with `>=64` permutations/null family and attach per-row diagnostics (orientation entropy, sign balance, kNN density, degree assortativity); model `delta_AUROC` vs diagnostics. | Global positive signal remains (`>=4/6` Fisher-significant groups) and negative rows cluster in identifiable low-information graph regimes. | Orientation-preserving rewires, sign-shuffle, split-label placebo, plus metric-matched kNN randomization. | high | medium |
| N242 | PH filtration variant | Signed extended persistence on separate positive/negative margin filtrations captures inhibitory/excitatory structure that plain directed/signed AUROC misses. | Build dual filtrations (`w+`, `w-`) from support-direction margins; compute extended PH summaries; test incremental value over `H52` features. | Positive incremental AUROC/calibration gain in multiple domain-splits, especially lung source. | Sign inversion, support-quantile shuffle, degree-preserving rewires. | high | high |
| N243 | Topology stability | Bootstrap recurrence frequency of persistent generators is more predictive than raw lifetime magnitude. | For each domain/seed/split/layer, run node/edge bootstraps (`>=32`), track generator participation frequency per edge/gene, compare against lifetime-only baseline. | Recurrence features improve held-out ranking and calibration reliability. | Matched-size random generator assignments and bootstrap label permutations. | medium | medium |
| N244 | Directed topology (major rescue) | Path-homology becomes useful when using denser directed complexes and a utility-first objective instead of raw discrimination only. | Rebuild path-homology with denser directed flags (`k` sweep, 2-3 step path features), then evaluate utility transfer (not just AUROC) across domains/models. | Significant utility lift in `>=2` domains and positive cross-model transfer vs baselines. | Directed degree-preserving rewires, sign-shuffle, random-map transfer controls. | high | high |
| N245 | Zigzag topology | Relative zigzag persistence across adjacent layer windows captures representation phase shifts that predict utility peaks. | Compute true zigzag on layer windows (`0-3`, `3-7`, `7-11`) and derive transition scores; regress against layer utility and edge performance. | Transition scores significantly track utility peaks better than static zigzag totals. | Layer-order shuffle and within-layer rewiring controls. | medium | medium |
| N246 | Manifold geometry (curvature rescue) | Curvature becomes informative only through interactions with support and detour context, not as a direct score. | Compute Ollivier/Forman curvature and fit nested models with `curvature x support` and `curvature x detour` terms. | Positive interaction coefficients with significant model gain in multiple domain-splits. | Curvature on rewired graphs, support-shuffle, detour-shuffle controls. | medium | medium |
| N247 | Manifold geometry (cheap screen) | Tail anisotropy of local geodesic neighborhoods is a stronger edge signal than mean geodesic distance statistics. | Compute local anisotropy metrics (eigenvalue ratios, tail quantiles) at edge endpoints for all seeds/domains/splits/layers and test incremental value over geodesic baseline. | Positive incremental gain in `>=4/6` domain-splits with at least one Fisher-significant domain. | Distance-matched endpoint swaps and neighborhood-permutation controls. | medium | low |
| N248 | Intrinsic dimension geometry | Steep local intrinsic-dimension drops along regulator-target geodesic paths mark true regulatory edges. | Estimate local ID (TWO-NN + MLE), compute pathwise ID gradient features for candidate edges, compare vs existing geometry baseline. | True edges show stronger negative ID-gradient tails and improved ranking. | Endpoint shuffles within degree bins and estimator-randomization placebo. | medium | low |
| N249 | Cross-model topology transfer | Persistence-image alignment across scGPT and Geneformer transfers predictive topology without exact one-to-one correspondences. | Build persistence images per domain/layer/model, align with CCA/Procrustes on matched modules, and evaluate transferred edge priors. | Transfer AUROC above random-map and motif baselines in `>=2` domains. | Random map permutations and within-model layer shuffles. | high | medium |
| N250 | Cross-model structure transfer | OT on topological signature vectors (not raw embeddings) yields more stable cross-model transfer utility. | Construct per-gene/module signature vectors (`H47/H50/H44` summaries), solve entropic OT, and test transferred utility on held-out domains. | Improved transfer utility and top-k overlap stability vs independent/random couplings. | Cost-matrix permutation and random-coupling controls. | high | high |
| N251 | Biological anchoring | High-recurrence persistent-generator genes are enriched for TRRUST/GO/STRING programs beyond degree/expression expectations. | Aggregate top generator genes from `H52/H44`-style runs, run matched enrichment by domain/split/layer. | Reproducible enrichment with coherent domain-specific biological themes. | Degree-expression matched random sets and generator-label shuffles. | high | low |
| N252 | Biological anchoring (failure-slice diagnosis) | The `lung/source_disjoint` negative slice is concentrated in low-support or ontology-incoherent edges and can be neutralized by biologically grounded weighting. | Stratify `H52` edges by STRING confidence, TRRUST evidence, and ontology-distance; re-estimate weighted directed/signed scores by stratum. | Negative slice localizes to low-support strata; weighted score restores positive mean delta. | Biological-score shuffles within degree bins and random stratum assignments. | high | low |
| N253 | Mechanistic signatures | Topological hotspot edges with low commutator drift of local layer-transition operators represent reusable regulatory circuits. | Estimate local linear operators around hotspot neighborhoods and compute commutator norms across adjacent layers; test as predictors. | Lower commutator drift enriches positives and improves calibration over topology-only features. | Random hotspot labels and layer-order permutation controls. | high | high |
| N254 | Mechanistic motifs | Discrete Morse critical edges that persist across filtration scales identify mechanistic control points in representation space. | Build critical-edge sets from filtration landscapes and use critical persistence as edge/module features across layers/seeds. | Critical persistence enriches positives and overlaps known regulatory programs. | Degree-preserving edge-weight permutation and matched-size random critical sets. | medium | high |

## Top 3 for Immediate Execution

| Role | Candidate | Why this now | Minimal success gate |
|---|---|---|---|
| High-probability discovery | `N241` (`H55`) | Extends the strongest validated branch (`H52`) while directly diagnosing its only failure slice. | `delta_AUROC>0` in `>=75%` rows, Fisher-significant in `>=4/6` domain-splits, and explicit explanatory model for `lung/source_disjoint` negatives. |
| High-risk / high-reward | `N244` (`H56`) | Gives path-homology one real rescue with a materially different objective and richer directed construction. | Significant utility/transfer lift in `>=2` domains under directed nulls; otherwise retire path-homology branch. |
| Cheap broad-screen | `N247` (`H57`) | Low-cost manifold feature screen with broad coverage and immediate fallback value if high-risk slot fails. | Positive incremental gain in `>=4/6` domain-splits with at least one domain-level Fisher-significant aggregate. |
