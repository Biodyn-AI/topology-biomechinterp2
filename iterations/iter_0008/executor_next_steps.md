# Executor Next Steps — iter_0008

1. Run a bridge-identifiable calibration rerun by forcing overlap of bridged/non-bridged rows within each split (for example fixed low-k and high-k schedules per split), then recompute bridge-conditioned H1 deltas.
2. Replicate the quantile-constrained rewiring packet in external-lung (`N34`) with the same schema to test whether immune-negative rewiring behavior is domain-general.
3. If rewiring remains uniformly negative after the identifiable rerun and one cross-domain replication, close the rewiring-survival branch and shift effort to biological anchoring of robust feature-shuffle layers (`7,9,10,11`; `N41`).
