# Executor Next Steps: iter_0047

1. Keep `H126` active and run one higher-null-budget confirmation (`>=48`) focused on source-disjoint slices at layers `{7,11}`, prioritizing `external_lung/source_disjoint` where null-gap remained negative.
2. Keep `H124` as a refinement branch but require explicit robustness targets before promotion: recover `lung/dual_axis_disjoint` to positive mean null-gap and lift total positive-null-gap domain-splits from `4/9` toward `>=8/9`.
3. Retire `H125` endpoint (anchor-constrained cycle map with current signatures/objective); do not spend another slot on near-identical cross-model alignment without a materially new target.
4. Next breadth packet should include one `H124` refinement slot, one new topology/graph-topology slot, and one non-retired manifold slot with strengthened nulls.
