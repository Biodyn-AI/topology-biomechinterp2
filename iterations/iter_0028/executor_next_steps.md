# Executor Next Steps — iter_0028

1. Promote one focused follow-up on `H69` with stronger null resolution (`>=32` permutations per null family) and seed/layer bootstrap CIs; keep the same domains/splits so effect stability can be compared directly to `iter_0028`.
2. Keep `H67` and `H68` retired in their current utility endpoint forms; do not allocate next-iteration budget to minor tweaks of these two branches.
3. Fill remaining next-iteration slots with non-retired high-novelty families (`module_structure` or `graph_topology` with biological anchoring) plus one split-robustness stress test for the `H69` feature set.
