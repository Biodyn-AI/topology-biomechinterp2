# Brainstormer Hypothesis Roadmap - iter_0028

## Retire / Deprioritize
| Direction | Status | Why | Re-entry condition |
|---|---|---|---|
| Multiparameter persistence AUROC-lift forms (`H64`, `H67`) | `retire_now` | Two consecutive major variants are negative across all domain-splits. | Only if endpoint changes away from direct AUROC lift. |
| Cross-model global-map utility transfer (`H59`, `H62`, `H65`, `H68`) | `retire_now` | Repeated null/utility mismatch; latest run is strongly negative in every domain-split. | Only if representation objective changes to topology-signature transfer with explicit utility constraints. |
| Standalone intrinsic-dimension utility endpoints (`H54`, `H60`, `H63`, `H66`) | `retire_now` | Four consecutive negative outcomes. | Re-open only as interaction/gating features. |
| Path-homology utility-transfer endpoint (`H53`, `H56`) | `retire_now` | Utility-transfer gate repeatedly failed. | New endpoint and transfer target required. |
| Weight-polish rescue variants (`H46`, `H58`) | `retire_now` | Did not improve known source-disjoint failure slices. | Only after a new core representation is introduced. |
| Biological anchoring as post-hoc interaction-only endpoint (`H40`, `H43`) | `rescue_once_with_major_change` | Strong interaction coefficients but near-zero direct utility gain. | Move biology into filtration/stratified representation, not only regression terms. |
| Cross-model family overall | `rescue_once_with_major_change` | Global mapping forms failed, but earlier consistency results suggest recoverable structure. | One final representation-first attempt; retire family if utility is still non-positive. |

## New Hypothesis Portfolio
| ID | Track | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N343 | manifold_geometry | The `H69` triangle-defect gain is real and survives hard-null resolution once permutation budget is increased. | Re-run `H69` on all domains/seeds/splits/layers `{7,11}` with `>=48` permutations per null family and bootstrap CIs for each domain-split-layer. | Positive mean delta in all or most domain-splits and positive matched-random-third null-gap in at least half of domain-splits. | Same three nulls as `H69` plus split-label swap placebo. | high | low |
| N344 | topology | Defect-weighted persistence (using triangle-defect as filtration weight) captures structure missed by rank-surface features. | Build edge filtrations weighted by geodesic + defect quantiles; compute persistence-image features; test incremental value over `H69` baseline. | Source-disjoint slices (lung/external-lung) improve without collapsing immune performance. | Defect-value shuffle within geodesic bins, label permutation. | high | medium |
| N345 | topology_stability | Zigzag vineyard drift across `source->union<-target` is a stronger robustness marker than static lifetime sums. | Compute vineyard birth/death drift per layer transition; add drift features to directed baseline and test split robustness. | Positive edges show lower drift variance and improved calibration. | Layer-order permutation and source/target swap controls. | medium | high |
| N346 | topology | Persistent-Laplacian spectral gaps in local edge complexes encode regulatory closure beyond cycle counts. | For each candidate edge neighborhood, compute persistent Laplacian eigen-gap trajectories and use as features in nested utility model. | Significant incremental utility in at least two domains. | Degree-preserving local rewiring and filtration-order randomization. | medium | medium |
| N347 | manifold_geometry | Multiscale chord-vs-arc distortion ratios provide cheap geometric lift complementary to triangle-defect scores. | Compute chord/arc ratios on local geodesic loops for `k={6,10,14,18}` and append to `H69` baseline. | Non-negative global mean lift and reduced negative tails in weak slices. | Endpoint swap within geodesic bins, random third-node controls. | medium | low |
| N348 | manifold_geometry | Tangent transport entropy across layers is lower for true regulatory edges than null edges. | Estimate local tangent bases per layer and edge-wise transport entropy across transitions (`0->3->7->11`), then test incremental utility. | Positives have lower entropy; strongest effect in late transitions. | Neighborhood permutation preserving degree/density and transition-order shuffle. | medium | medium |
| N349 | manifold_geometry + graph | Dynamic curvature (layer slope/acceleration) interacts with support margin even when static curvature fails. | Compute curvature per layer, derive slope/acceleration terms, and fit interaction model with margin and defect features. | Positive interaction coefficients and recovery in source-disjoint slices. | Layer-order permutation and curvature shuffle within degree bins. | medium | medium |
| N350 | cross_model_alignment | Cross-model transfer improves if Geneformer is distilled into scGPT topology-signature space rather than aligned by global embedding maps. | Train domain-held-out mapping from GF features to scGPT topology signatures (`H69`/directed features), evaluate transfer utility on held-out domain. | Immune and at least one non-immune domain achieve non-negative transfer delta with positive null-gap. | Random teacher signatures, anchor shuffle, signature-destroy permutation. | high | high |
| N351 | cross_model_alignment | Geodesic-landmark skeleton matching transfers structure more robustly than token/codebook transport. | Build landmark graphs per model, align via distance-matrix matching, score edges by landmark path compatibility. | Better stability across layers and improved non-immune transfer compared with `H65/H68` forms. | Landmark identity permutation and random landmark-distance controls. | medium | medium |
| N352 | cross_model_alignment + biology | Alignment within biological strata (TRRUST confidence/GO module/cell ontology) outperforms a single global cross-model map. | Fit per-stratum maps and ensemble transfer predictions by stratum confidence; compare with global map baseline. | Transfer gains concentrated in high-support strata and non-negative global delta. | Stratum-label permutation and random stratum-weight controls. | high | medium |
| N353 | biological_anchoring | `H69` gains are strongest for edges with convergent support (TRRUST + STRING + GO) and this conditioning rescues immune/source weakness. | Fit stratified models with triangle-defect interactions against support score bins; test per-stratum and global utility impact. | Positive defect-support interaction and stronger immune/source domain-split mean delta. | Support-score shuffle within degree bins and matched random-support controls. | high | low |
| N354 | biological_anchoring + topology | Adding GO semantic proximity as a filtration axis isolates biologically coherent cycles that improve hard-split utility. | Extend filtration to `(distance, defect, GO-semantic distance)` with rank-surface summaries; test incremental value vs `H69`. | Source-disjoint improvements with preserved target-disjoint gains. | GO-term permutation with size-frequency matching. | high | high |
| N355 | algorithmic_signature | Edge trajectory motifs across layers (early-closing vs late-closing triangle-defect patterns) encode mechanistic regulation classes. | Build layerwise trajectories for defect/topology features, cluster motifs, test enrichment and add motif class as feature. | At least one motif class significantly enriches positives and improves calibration. | Layer-order permutation and class-label shuffle within degree bins. | medium | low |
| N356 | mechanistic_motif | Counterfactual deletion of top-scoring geometry/topology edges causes larger module-coherence loss than matched random deletions. | Remove top-q scored edges from inferred regulatory graph and measure module conductance/coherence against matched random removals. | Stronger coherence drop for scored edges, especially in high-support modules. | Degree-matched random edge deletion and score-permutation controls. | high | medium |

## Top 3 for Immediate Execution
| Role | Candidate | Why now | Minimal success gate |
|---|---|---|---|
| High-probability discovery candidate | N343 | Extends the only active positive branch (`H69`) and directly resolves the current statistical-resolution caveat. | Global mean `delta_AUROC >= +0.015`, positive mean in `>=5/6` domain-splits, and matched-random-third null-gap positive in `>=3/6`. |
| High-risk/high-reward candidate | N350 | Cross-model remains high upside; this is a materially different representation-first transfer objective. | Immune plus at least one non-immune domain has both `delta_AUROC >= 0` and `null_gap_q95 > 0`. |
| Cheap broad-screen candidate | N355 | Low implementation cost and broad mechanistic coverage using already-computed layerwise features. | At least one motif class enrichment survives permutation (`p<0.05`) and adding motif feature yields non-negative mean utility delta. |
