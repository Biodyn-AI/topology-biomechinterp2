# Brainstormer Hypothesis Roadmap - iter_0019

## Retire / Deprioritize
| Direction | Evidence | Decision |
|---|---|---|
| Rewiring-null survival lineage (`H07/H09/H11/H12`) | Multi-iteration negative outcomes after calibration and constrained rewiring variants | `retire_now` |
| GW-first edge correspondence (`H27/H29`) | Repeated failures in top-1 recovery and transfer AUROC | `retire_now` |
| Raw curvature/thinness direct scores (`H23/H30`) | Consistent below-baseline or opposite-direction results | `retire_now` |
| Coarse discrete tier formulations (`H19/H37`) | Opposite-direction slopes or sparse/non-identifiable tier gaps | `retire_now` |
| Pooled H38 claim in current OOS metric form (`H42`) | Mean OOS `delta R^2` strongly negative with holdout instability | `retire_now` |
| Split-zigzag proxy branch (`H41`) | Directional signal but zero robust significance under control; proxy tooling limitation | `rescue_once_with_major_change` |
| Anchor-causality interpretation (`H36`) | Utility gain present, causal attribution unresolved by current nulls | `rescue_once_with_major_change` |
| Diffusion incremental pooled framing (`H28/H31`) | Small positives with weak cross-domain robustness when pooled | `rescue_once_with_major_change` |
| Cycle-consistency-first alignment (`H33`) | Structural metrics improved without transfer-utility lift | `rescue_once_with_major_change` |

## New Hypothesis Portfolio
| ID | Theme | One-sentence hypothesis | Concrete test design | Expected signal if true | Null / control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N183 | Topology (bifiltration) | Regulatory edges are enriched in cycle structure that persists jointly over distance and biological-support filtrations. | Build a two-parameter filtration (`distance radius`, `support score`) and summarize persistent mass/Betti surface features per edge neighborhood; test incremental value over `geodesic+diffusion+detour+convexity`. | Positive coefficients for high-support persistent mass in `>=2` domains. | Permute support within degree x coexpression x geodesic strata; shuffle one filtration axis. | high | high |
| N184 | Topology (filtration variants) | True signal survives across Rips, witness, and DTM filtrations and is stronger in filtration-consensus features than in any single filtration. | Compute H1 summaries under three filtrations on the same rows, aggregate by rank-consensus, compare against single-filtration models. | Consensus model outperforms best single filtration in AUROC/log-loss. | Filtration-label swap and feature-shuffle nulls. | high | medium |
| N185 | Topology (true zigzag) | Split-invariant regulatory cycles are detectable only with true zigzag persistence across source/target complexes. | Use shared landmarks to build split complexes, run zigzag persistence across split transitions, derive edge-level zigzag features, and compare with geodesic baseline. | Positive zigzag delta with at least one domain-split Fisher-significant aggregate. | Split swap, layer-order permutation, and landmark randomization controls. | high | high |
| N186 | Topology + biology | Genes participating in representative persistent cycles are biologically coherent (TRRUST/GO/STRING enriched) beyond chance. | Extract representative cocycles/cycles for persistent H1 classes, map high-contribution genes, run enrichment tests by layer/domain. | Significant enrichment concentrated in layers with high edge-utility uplift. | Degree-matched random gene sets and cycle-membership permutation. | medium | medium |
| N187 | Topology stability | Edge utility tracks PH stability (low bootstrap bottleneck variance) more than raw persistence magnitude. | Bootstrap neighborhoods, compute bottleneck/Wasserstein stability metrics, add them to edge models with persistence magnitude controls. | Negative instability effect and improved calibration at fixed discrimination. | Bootstrap-index randomization and magnitude-decile matched random stability labels. | medium | medium |
| N188 | Manifold geometry (curvature) | Curvature becomes predictive only after removing degree/coexpression/geodesic nuisance structure. | Compute Ollivier-Ricci (or validated proxy), residualize against nuisance covariates, and test residual curvature terms in nested models. | Residual curvature adds positive incremental value in `>=2` domain-splits. | Degree-preserving rewiring with curvature recomputation. | medium | medium |
| N189 | Manifold geometry (anisotropy) | Geodesic detour is most informative when endpoint neighborhoods have aligned anisotropy axes. | Estimate local covariance eigensystems; create anisotropy ratio and principal-angle features; test detour x anisotropy interactions. | Positive interaction and consistent gain over detour-only models. | Neighbor-index permutation preserving node degree/local density. | medium | medium |
| N190 | Manifold geometry (ID dynamics) | Edge labels depend on local intrinsic-dimension gradients across neighborhoods rather than absolute ID values. | Compute node-level TWO-NN/PR IDs and edgewise ID-gradient features across layers; evaluate mixed-effects edge models. | Significant ID-gradient coefficients with reproducible sign across domains. | Layer-label permutation and neighborhood reassignment controls. | medium | low |
| N191 | Intrinsic/OOS robustness | If the ID mechanism is real, it survives robust OOS metrics (MAE and winsorized `R^2`) even when raw `R^2` is unstable. | Reuse H38 features; evaluate leave-layer-out and leave-seed-out with MAE, trimmed/winsorized `R^2`, and robust regression. | Positive robust OOS gain in `>=2` domain-splits with narrower variance than H42. | Target permutation and block-bootstrap holdout controls. | high | low |
| N192 | Manifold geometry (adaptive diffusion) | A locally adaptive diffusion time field outperforms any fixed diffusion time for edge discrimination. | Learn per-edge `t*` from local ID/curvature/anisotropy predictors, compute adaptive diffusion distances, compare to fixed-`t` sweep. | Adaptive minus best-fixed AUROC/log-loss improvement with consistent sign. | Randomized `t` assignments matched to learned `t*` distribution. | high | medium |
| N193 | Cross-model topology transfer | Cross-model transfer quality is driven by persistence-image similarity, not pointwise correspondence recovery. | Build persistence images per model/layer/domain on matched genes, align models via CCA/Procrustes, regress transfer AUROC on PI similarity. | Strong positive PI-similarity slope and significant domain-level association. | Gene correspondence permutation and PI phase-scramble controls. | high | medium |
| N194 | Cross-model alignment objective | Adding topological regularization to spectral alignment improves transfer utility and interpretability over utility-only alignment. | Extend H36 objective with homology-preservation penalty between model graphs; tune on source split, evaluate target split utility. | Higher target AUROC plus stronger anchor-sensitivity than baseline H36. | Same pipeline with shuffled topology penalty and random anchors. | high | high |
| N195 | Biological anchoring (STRING + ontology) | H40 interaction strengthens and generalizes when true STRING scores and cell-ontology random effects are added. | Integrate STRING edge scores into support, add ontology-stratified mixed effects, rerun interaction packet over seeds/domains/splits/layers. | Positive interaction in `>=2/3` domains with lung improvement over current H40. | Support permutation within degree x coexpression x geodesic x ontology bins. | high | medium |
| N196 | Biological anchoring (discordance mining) | Geometry identifies context-specific edges primarily where TRRUST and STRING priors disagree. | Partition edges by prior concordance/discordance, test geometry uplift and GO enrichment separately in each partition. | Discordant-high-geometry subset shows strongest tissue-specific GO enrichment. | Concordance-label shuffle within matched degree/coexpression strata. | medium | low |
| N197 | Mechanistic motifs | Regulatory edges are enriched for relay/feed-forward motif participation in representation-space graphs. | Detect triad/relay motifs in kNN graphs and derive edge motif-load features; test incremental value beyond distance/topology covariates. | Positive motif-load coefficients and enrichment in known TF modules/pathways. | Motif-preserving rewiring and degree-matched random-chain controls. | high | medium |

## Top 3 for Immediate Execution
1. High-probability discovery candidate: **N195 (STRING + ontology extension of H40)**.
- Why now: `H40` already clears signal thresholds (`4/6` significant domain-splits) and has a concrete missing component (true STRING scores).
- Promotion gate: positive interaction plus positive top-decile uplift in at least `2/3` domains, with lung no longer negative in both splits.

2. High-risk/high-reward candidate: **N185 (true split-zigzag persistence)**.
- Why now: proxy zigzag is directionally positive but method-limited; a real zigzag run can open a genuinely new topology axis.
- Promotion gate: at least one domain-split Fisher-significant zigzag gain with consistent direction across layers.

3. Cheap broad-screen candidate: **N191 (robust OOS ID screen)**.
- Why now: fastest way to kill or rescue ID claims after `H42` without new heavy feature engineering.
- Promotion gate: robust OOS metrics (MAE / winsorized `R^2`) beat permutation controls in `>=2` domain-splits.
