# Executor Next Steps: iter_0049

1. Retire the tested cross-model chart/sheaf endpoint (`H131`) unless a materially different cross-model signal is used (for example correspondence-free topology signatures instead of transfer-utility deltas).
2. Keep `H130` as the only active refinement branch, but constrain follow-up to hard-slice diagnostics (`lung/dual_axis_disjoint`, `immune/source_disjoint`) with stronger semantic controls rather than broad reruns.
3. Retire the tested chart-fracture manifold endpoint (`H132`) in its current feature/null design; only revisit manifold geometry with a different representation target (for example path-homology or Ricci-flow asymmetry).
4. Next breadth packet should prioritize non-stale families to avoid over-investment in repeated null-negative branches (for example one topology family slot + one null-sensitivity slot + one module-structure slot).
