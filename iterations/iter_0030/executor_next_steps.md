# Executor Next Steps — iter_0030

1. Promote only the robust geometry branch (`H70` lineage) and redesign the biological-anchor test with explicit coexpression-aware matching; treat `H73` interaction formula as non-promotable.
2. Retire cross-model edge-utility transfer again after `H74`; if cross-model is revisited, switch endpoint to non-edge relational consistency or biology-stratified ranking only.
3. Keep `H75` as a cheap unresolved probe; rerun only with materially changed curvature parameterization and a strict null-gap gate (`null_gap_q95 > 0` in at least one domain-split).
