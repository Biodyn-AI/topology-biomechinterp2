# Brainstormer Hypothesis Roadmap - iter_0030

## Retire / Deprioritize
| Direction | Status | Why | Re-entry condition |
|---|---|---|---|
| Cross-model edge-utility transfer (`H29/H33/H59/H62/H65/H68/H71/H74`) | `retire_now` | Too many redesigns, still utility-negative or null-gap-negative, especially immune. | Re-open only with a non-edge endpoint (relational agreement / motif consistency). |
| Rewiring-survival stronger-null branch (`iter_0005`-`iter_0008`) | `retire_now` | Uniformly non-supportive across calibrated variants. | Only if graph-construction regime changes materially. |
| Standalone intrinsic-dimension utility lifts (`H42/H54/H60/H63/H66`) | `retire_now` | Repeated negatives with no reproducible rescue slice. | Re-open as interaction features with geometry/topology, not standalone. |
| Two-axis/rank-surface persistence utility forms (`H64/H67`) | `retire_now` | Consecutive all-split negatives. | Re-open only for localization/stability endpoint. |
| Path-homology utility-transfer form (`H53/H56`) | `retire_now` | Directional discrimination without utility transfer; failed objective twice. | Re-open only with local mechanistic endpoint (not transfer utility). |
| Support-interaction formula as tested in `H58/H73` | `rescue_once_with_major_change` | Past support branch had signal (`H40/H43`), but current interaction + null calibration fails. | One coexpression-aware + cell-ontology-aware redesign with strict null-gap gate. |
| Curvature variants as currently parameterized (`H23/H61/H75`) | `rescue_once_with_major_change` | Static/acceleration formulations not robust under null-gap. | One discrete-Ricci/transport redesign; retire if still non-robust. |

## New Hypothesis Portfolio
| ID | Track | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N371 | topology | Edges with high `H70` lift sit in locally stable split-zigzag neighborhoods rather than isolated high-score pairs. | For each edge, build local ego complexes in `source->union<-target` and compute true zigzag stability features (lifetime retention, barcode overlap) at layers `{7,11}` across domains/seeds/splits; test incremental lift over `H70`. | Positive mean incremental AUROC and strongest gain in immune/source where raw lift is weakest. | Edge-set randomization matched on geodesic/support, split swap, and label shuffle. | high | medium |
| N372 | topology | Direction-aware path homology asymmetry (forward vs reverse) captures regulatory polarity missed by symmetric triangle-defect features. | Compute local directed path-homology summaries around each edge and include forward-minus-reverse persistence asymmetry as features on top of baseline geometry. | Positive edges show higher forward asymmetry; null-surviving uplift in at least 3/6 domain-splits. | Edge-direction randomization, sign shuffle, label permutation. | medium | high |
| N373 | topology | Witness-complex persistence stability across landmark subsamples is higher for true regulatory edges. | Build witness complexes around edge neighborhoods with repeated landmark subsampling; use persistence-landscape variance/Wasserstein drift as features. | Lower drift for positives and robust top-k precision lift. | Random landmark sets matched by distance spectrum, plus label shuffle. | medium | medium |
| N374 | topology_stability | Persistence-vineyard drift across layers (`0,3,7,11`) is lower for mechanistically real edges. | Compute vineyard event-count and drift amplitude per edge neighborhood, then test incremental utility over geodesic baseline. | Positives have lower drift and better calibration in target-disjoint splits. | Layer-order permutation and barcode time-scramble controls. | medium | high |
| N375 | manifold_geometry | Discrete Ricci curvature flow trajectories distinguish true edges better than curvature-acceleration summary statistics. | Compute Ollivier/Forman curvature per layer on kNN graphs; derive flow descriptors (mean, min, slope, sign changes) and test lift over `H70` baseline. | Positive global delta and at least one null-surviving immune split. | Degree-preserving rewiring within geodesic bins and layer-order permutation. | high | medium |
| N376 | manifold_geometry | True edges are geodesically robust: detour elasticity under local neighbor dropout is lower than for matched negatives. | For each edge, perturb local neighborhoods (dropout schedule), recompute shortest-path detour ratio across layers, and add elasticity descriptors to baseline model. | Lower elasticity for positives and consistent non-negative deltas in most domain-splits. | Matched endpoint randomization with identical dropout budgets and label shuffle. | medium | low |
| N377 | manifold_geometry | Endpoint tangent-basis transport inconsistency across layers is lower for real regulatory edges. | Estimate local PCA tangent bases per endpoint/layer and compute principal-angle transport inconsistency and its depth trend. | Positives show lower inconsistency and improved ranking in immune/target. | Neighborhood permutation preserving degree/density, layer-order permutation. | medium | medium |
| N378 | intrinsic_dimension | ID gradient sign-switch motifs across layers encode mechanistic classes linked to regulatory edges. | Compute TWO-NN/MLE local ID at endpoints across layers, derive sign-switch motifs (e.g., down-up, flat-down), and test motif enrichment + predictive lift. | At least one motif class significantly enriched with positive precision@k contribution. | Layer-order shuffle, motif-label shuffle within geodesic bins. | medium | low |
| N379 | cross_model_alignment | Cross-model structure is recoverable as relational rank agreement even when direct edge transfer fails. | Build model-specific topology score matrices and evaluate held-out-domain rank concordance (Kendall/Spearman, top-k overlap) as the primary endpoint instead of transfer AUROC. | Consistent positive concordance with null-gap >0 in immune and at least one non-immune domain. | Symbol permutation, spectral-basis randomization, and signature destroy controls. | high | medium |
| N380 | cross_model_alignment + biology | Alignment quality improves when learned/evaluated within biological strata (TRRUST/GO/cell ontology) rather than globally. | Partition genes/edges into biological strata, fit stratum-specific maps, and evaluate cycle-consistency and neighborhood overlap per stratum. | High-confidence strata show significant gains; pooled metric improves over global map. | Stratum-label permutation and random stratum weighting controls. | medium | medium |
| N381 | cross_model_mechanistic | Motif-state frequency transfer (not edge scores) is a stable cross-model mechanistic invariant. | Build layer-trajectory motif-state models in both architectures and measure divergence of state distributions per domain/stratum. | Low divergence relative to shuffled mappings with reproducible domain structure. | State relabel shuffle, domain shuffle, degree-preserving graph rewiring. | medium | low |
| N382 | biological_anchoring | `H70` lift is genuinely biological only when support-concordance is stratified by coexpression and ontology consistency. | Redo support interaction with strata = geodesic x degree x coexpression x ontology tier; evaluate high-vs-low support `delta_AUROC` interaction and null-gap. | Global interaction >0 and `>=3/6` domain-splits with positive interaction null-gap; immune/source improves above zero. | Within-strata support shuffle, matched random-support resampling, label shuffle. | high | medium |
| N383 | biological_anchoring | Cell-ontology concordance between edge endpoints predicts where topology lift is trustworthy. | Annotate endpoints with cell-ontology terms, compute concordance score, and test topology-lift interaction by concordance tier. | High-concordance tier has larger topology lift and better calibration consistency. | Ontology-term permutation preserving frequency and tier-size matched randomization. | medium | medium |
| N384 | algorithmic_motifs | Edge trajectory automata (state transitions over layers) reveal mechanistic motif classes with predictive value. | Quantize per-edge layerwise geometry/support signals into states, mine transition motifs, and test motif-membership features for incremental utility. | At least one motif class null-surviving enriched and improves top-k precision. | Transition shuffle within geodesic bins, layer-order permutation, label shuffle. | medium | low |

## Top 3 for Immediate Execution
| Role | Candidate | Why now | Minimal success gate |
|---|---|---|---|
| High-probability discovery candidate | N382 | Directly addresses the known `H73` failure mode (missing coexpression-aware stratification) while leveraging robust `H70` signal. | Global interaction >0, immune/source `delta_AUROC > 0`, and positive interaction `null_gap_q95` in `>=3/6` domain-splits. |
| High-risk/high-reward candidate | N379 | Gives cross-model one final shot with a materially different endpoint that avoids repeated edge-transfer failure mode. | Immune plus at least one non-immune domain show concordance metric above null q95 and positive effect direction. |
| Cheap broad-screen candidate | N376 | Low implementation cost, broad coverage, and directly probes manifold robustness with clear controls. | Non-negative global mean delta, positive mean in `>=4/6` domain-splits, and at least one permutation-significant split. |
