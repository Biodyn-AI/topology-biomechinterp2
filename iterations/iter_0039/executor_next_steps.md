# Executor Next Steps - iter_0039

1. Retire the exact `H100` relative-persistence contrast formulation for promotion claims unless a materially changed anchor/background construction is introduced (current run: `0/6` positive mean null-gap domain-splits).
2. Keep `H101` as a one-time rescue candidate only if changed materially (e.g., interaction-only integration with robust `H91/H93` backbones rather than additive AUROC blend); otherwise treat current additive derivative-spectrum form as non-promotable.
3. Re-retire cross-model alignment after `H102` fast-fail (`0/3` positive q95 null-gap domains) and do not allocate more than one future slot without a fundamentally new shared representation anchor.
4. Next packet should prioritize non-retired high-upside in-model directions (e.g., local homology, hierarchical module pooling, or split-robustness confirmations) with explicit null-gap gates and at most one carry-over refinement.
