# Executor Next Steps - iter_0037

1. Keep exactly one follow-up on `H95`: rerun the same graph-topology method with higher null resolution (at least 24-32 rewiring draws per row) and stricter rewiring diagnostics to decide promotion vs retirement decisively.
2. Do not spend another immediate slot on `H94` GO-stratified additive rescue; keep the global weighted-filtration branch as the persistent-homology baseline and redirect budget to materially new topology formulations.
3. Mark `H96` as retired for now and avoid further cross-model endpoint variants unless new shared residual-level supervision or matched embedding tensors are introduced.
4. Next packet should prioritize breadth: at least two non-retired families (`persistent_homology` new method + `null_sensitivity` or `intrinsic_dimensionality` major-change non-additive design) with explicit null controls.
