# Executor Next Steps - iter_0036

1. Promote `H91` to a confirmatory high-resolution run (same design, higher null budget) and lock a compact descriptor core for downstream mechanistic interpretation.
2. Promote `H93` to multiseed replication (`42/43/44`) with increased null draws and an explicit adaptive-threshold variant to test whether weighted-filtration gains remain robust.
3. Retire the tested `H92` additive scale-trajectory utility formulation; if revisited, use scale-space descriptors only as diagnostics/stratifiers on top of already-positive backbones.
4. Keep breadth in the next packet: at most one `H91/H93` carry-over slot and at least one materially novel family outside recently retired standalone additive formulations.
