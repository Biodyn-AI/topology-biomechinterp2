# Brainstormer Hypothesis Roadmap - iter_0027

## Retire / Deprioritize
| Direction | Status | Why | Re-entry condition |
|---|---|---|---|
| Two-axis persistence AUROC-lift (`H64` form) | `retire_now` | Uniform negative utility (`0/6` positive domain-split means). | Only if endpoint changes away from direct AUROC lift. |
| Standalone intrinsic-dimension endpoints (`H54/H60/H63/H66`) | `retire_now` | Repeated negative outcomes across new/rescue variants. | Re-open only as interaction or gating feature. |
| Path-homology utility-transfer (`H53/H56`) | `retire_now` | Utility-transfer gate repeatedly fails despite directional discrimination. | New transfer objective plus new feature family required. |
| Weighted directed/signed rescue tweaks (`H58` and related weighting polish) | `retire_now` | Did not rescue source-disjoint failures. | Only if underlying topology representation changes. |
| Static curvature direct scores (`H23/H61`) | `retire_now` | Net-negative and unstable across domains. | Re-open only as dynamic curvature interactions. |
| Cross-model transfer utility forms (`H59/H62/H65`) | `rescue_once_with_major_change` | Structure/null effects exist but practical transfer utility is non-robust or negative. | One final cycle-consistent, utility-regularized redesign. |
| Bifiltration utility-coupling claim (`H49` endpoint form) | `rescue_once_with_major_change` | Discrimination is robust; utility coupling remains weak/placebo-sensitive. | One redesigned utility endpoint (ranking/calibration or perturbation alignment). |

## New Hypothesis Portfolio
| ID | Track | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N329 | topology | Rank-based multiparameter persistence surfaces over `(distance rank, support-margin rank)` recover source-disjoint failures that quantile-threshold bifiltration missed. | Across domains/seeds/splits/layers `{7,11}` then `{0,3,7,11}`, compute persistence images/landscapes from rank-bifiltration and add to `H55` baseline. | `lung/source_disjoint` and `external_lung/source_disjoint` mean incremental utility become non-negative with `>=4/6` positive domain-split means. | Degree-bin rank permutation on each axis, one-axis ablation, label permutation. | high | high |
| N330 | topology_stability | Zigzag vineyard drift across `source->union<-target` and depth transitions is a stronger robustness marker than static H1 lifetime. | Build vineyards across layers and splits; use drift velocity/turnover descriptors as edge-neighborhood features in a nested model. | Lower drift and lower turnover entropy correlate with positives and improve calibration slope. | Layer-order permutation, source/target union swap, bootstrap label shuffle. | medium | medium |
| N331 | topology | Persistent-Laplacian spectral gaps around local edge complexes encode regulatory closure better than raw cycle counts. | For each edge ego-complex, compute persistent Laplacian eigen-gap/heat-trace trajectories and test incremental gain over directed baseline. | Positive nested-model lift in at least two domains and improved source-disjoint slices. | Degree-preserving local rewiring and filtration-order randomization. | medium | medium |
| N332 | topology + biology | Relative homology of TF-centered complexes versus GO-matched background isolates causal TF-target edges. | Construct TF ego-complex and GO-matched background complex; compute relative Betti/lifetime features and evaluate on TRRUST-supported edges plus full set. | Stronger effect in TRRUST-supported strata and non-negative global utility delta. | TF-label shuffle within degree bins and GO-term swap controls. | medium | medium |
| N333 | mechanistic motifs | Directed motif persistence (FFL/bifan/feedback) provides mechanistic signal beyond pairwise topology scores. | Extract motif-conditioned local complexes, compute motif persistence vectors, and append motif-state features to edge scoring. | Specific motif persistence classes enrich positives and improve utility in all three domains. | Orientation/sign shuffle preserving motif counts and degree marginals. | medium | medium |
| N334 | manifold_geometry | Curvature dynamics (slope/acceleration across layers) interacting with margin are informative even when static curvature is not. | Compute per-edge curvature at each layer and derive slope/acceleration features with margin interactions; evaluate incremental gain. | Positive interaction coefficients and recovery in source-disjoint lung/external-lung slices. | Layer-order permutation and curvature-within-degree shuffles. | medium | low |
| N335 | manifold_geometry | Multiscale geodesic triangle-defect spectra are a cheap broad predictor of regulatory constraints. | For each edge, sample third nodes at `k={8,12,16}`, compute defect quantiles/skew/tails, and add to directed baseline. | Small but consistent gain (`>=4/6` positive domain-split means) with improved calibration error. | Endpoint swap within geodesic bins and random third-node matched controls. | medium | low |
| N336 | manifold_geometry | Tangent transport mismatch across layer transitions identifies non-regulatory pairs. | Fit local tangent bases per layer and compute principal-angle transport mismatch for each edge across `0->3->7->11`. | Positives show lower mismatch and late-layer (`7->11`) strongest separation. | Neighborhood permutation preserving density and transition-order shuffle. | medium | medium |
| N337 | intrinsic_dimension_interaction | Intrinsic-dimension volatility is useful only as a gating factor for topology scores, not as an additive endpoint feature. | Compute per-gene ID volatility across transitions and apply compatibility gating to `H55`/`N329` scores; compare against ungated versions. | Reduced negative tail in source-disjoint slices with no immune collapse. | Volatility shuffle within degree bins and random gating-weight baseline. | medium | low |
| N338 | cross_model_alignment | Cycle-consistent barycentric OT with explicit utility regularization can produce both positive null-gap and non-negative transfer utility. | Learn bidirectional scGPT↔Geneformer transport on two source domains with cycle loss + utility term; test held-out domain at layers `{7,11}`. | Immune and at least one non-immune split satisfy `null_gap_q95>0` and transfer delta `>=0`. | Random transport, anchor-label shuffle, signature-destroy permutation. | high | high |
| N339 | cross_model_alignment + biology | Cross-model transfer improves when alignment is done within biological strata (TRRUST confidence, GO modules, cell-ontology classes) instead of globally. | Fit per-stratum maps and ensemble edge scores by stratum confidence; compare to single global map. | Gains concentrate in high-support strata and raise global transfer utility to non-negative. | Stratum-label permutation and random stratum-weight controls. | high | medium |
| N340 | cross_model_alignment | Landmark-geodesic matching (correspondence-light) transfers structure better than codebook-token mapping. | Build model-specific geodesic landmark graphs, match via landmark GW/FGW, and score edges by landmark-path compatibility. | Higher non-immune transfer utility and lower layer variance than `H65`-type mapping. | Landmark identity permutation and random landmark distance matrices. | medium | medium |
| N341 | biological_anchoring + topology | Adding cell-ontology proximity as a filtration axis isolates conserved topology that is predictive in hard splits. | Extend filtration from `(distance, margin)` to `(distance, margin, ontology-proximity)` and evaluate utility per ontology stratum. | Largest gains in ontology-proximal strata and improved source-disjoint means. | Ontology-term permutation preserving frequency and size-matched random terms. | high | medium |
| N342 | algorithmic_signature | Edge score trajectory phase transitions across layers correspond to mechanistic regulation classes. | Track per-edge score trajectories (topology + geometry), cluster trajectory motifs, and test enrichment for TRRUST activation/repression classes. | One or more trajectory clusters show significant mechanism enrichment and improve calibration when used as features. | Layer-order permutation and mechanism-label shuffle within degree bins. | medium | low |

## Top 3 for Immediate Execution
| Role | Candidate | Why now | Minimal success gate |
|---|---|---|---|
| High-probability discovery candidate | N329 | Directly attacks the known source-disjoint failures while staying close to the strongest surviving topology signal. | Both source-disjoint failure slices non-negative and `>=4/6` positive domain-split means. |
| High-risk/high-reward candidate | N338 | The largest upside remaining is cross-model transfer, and this is the first design that explicitly constrains utility and cycle consistency together. | Immune `null_gap_q95>0` plus at least one non-immune split with `null_gap_q95>0` and transfer delta `>=0`. |
| Cheap broad-screen candidate | N335 | Very low implementation cost with broad geometric coverage and strong potential to expose overlooked manifold signal. | Non-negative global mean incremental utility and `>=4/6` positive domain-split means. |
