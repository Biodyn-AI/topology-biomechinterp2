# Executor Next Steps — iter_0027

1. Shift the next packet to non-retired families with high novelty (`module_structure`, `manifold_distance`, `topology_stability`) and require at least one biology-anchored endpoint (TRRUST/GO/STRING stratified) per hypothesis.
2. For topology, avoid AUROC-lift variants that repeatedly fail in source-disjoint slices; test calibration/ranking endpoints or perturbation-linked endpoints instead of direct edge AUROC deltas.
3. Keep cross-model work paused unless a materially different endpoint is introduced that jointly enforces null robustness and non-negative transfer utility.
