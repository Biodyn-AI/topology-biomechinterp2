# Executor Next Steps - iter_0034

1. Promote `H87` to a multiseed robustness pass (`42/43/44`) with increased null permutations and a stricter split-level gate (`>=5/6` positive mean null-gap domain-splits).
2. Keep `H85` as a bounded refinement only: target the failing slices (`external_lung/*`, `lung/target_disjoint`) via local-neighborhood calibration and rerun with the same keep gate (`>=4/6` positive mean null-gap domain-splits).
3. Retire `H86` in its current barcode-OT depth alignment form (`0/3` positive null-gap domains); reopen cross-model work only with a materially different topology-level anchor or domain-matched module regime.
4. Preserve breadth next iteration with one `H87` robustness slot, one `H85` targeted repair slot, and one orthogonal novelty slot outside retired standalone graph-topology/cross-model utility formulations.
