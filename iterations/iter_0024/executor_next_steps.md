# Executor Next Steps — iter_0024

1. Advance `H55` with a failure-slice rescue (`N252`-style): stratify/weight by STRING confidence + DoRothEA support + GO coherence and test whether `lung/source_disjoint` and `external_lung/source_disjoint` recover to non-negative mean `delta_AUROC`.
2. Rotate to a new family for breadth in iter_0025: run one cross-model topology-transfer hypothesis (`N249` or `N250`) with explicit random-map controls to avoid over-spending on the current topology-only branch.
3. Retire current `H56` utility-transfer and `H57` anisotropy-tail formulations; only re-open with materially changed objectives (not parameter tweaks).
