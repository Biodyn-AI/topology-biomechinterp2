# Brainstormer Hypothesis Roadmap - iter_0025

## Retire / Deprioritize
| Direction | Status | Evidence | Re-entry condition |
|---|---|---|---|
| Directed/signed weighting tweaks (`H58`-style) | `retire_now` | No rescue gain vs unweighted (`-0.00052` mean); source-disjoint failures remain negative. | Only with a different topology family (not another weighting variant). |
| Standalone endpoint ID-jump (`H60` + earlier endpoint ID lines) | `retire_now` | Net negative broad screen; positives concentrated in one late layer only. | Re-open only as layer-transition dynamics formulation. |
| Path-homology utility-transfer endpoint (`H53/H56`) | `retire_now` | Repeated inability to pass transfer/utility gate. | Re-open only with non-threshold transfer objective and different transfer target. |
| Standalone anisotropy-tail manifold endpoint (`H57`) | `retire_now` | Broad-screen keep gate failed and split/domain instability persists. | Only as interaction block in richer model. |
| Rewiring-survival null redesign branch (`H05-H12`) | `retire_now` | Multiple calibrated reruns stayed negative/non-discriminative. | Re-open only if tied to new biological endpoint, not new rewiring tweaks. |
| Cross-model raw Procrustes transfer objective (`H59` form) | `rescue_once_with_major_change` | Observed deltas are below random-map null deltas in most rows. | One rescue pass with null-gap objective and/or anchor supervision. |
| Bifiltration utility-coupling claim (`H49` endpoint) | `rescue_once_with_major_change` | Strong discrimination but weak utility linkage under placebo controls. | One rescue pass with ranking calibration transfer endpoint. |

## New Hypothesis Portfolio
| ID | One-sentence hypothesis | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|
| N301 | Multiparameter persistence over `(distance, directed-support margin)` will rescue source-disjoint failures that scalar directed/signed scores miss. | Build 2-parameter filtration on the existing kNN graph; extract persistence-image features per edge neighborhood; test incremental AUROC/calibration vs `H55` baseline on full domain-seed-split matrix. | Non-negative mean delta in `lung/source_disjoint` and `external_lung/source_disjoint`, with global positive mean in `>=4/6` domain-splits. | Margin permutation within degree bins; support permutation; 1-parameter filtration ablation. | high | high |
| N302 | Bootstrap/vineyard stability of cycle features is more predictive than raw persistence magnitude. | Run bootstrap node/edge resamples per split/layer; compute bottleneck variance and recurrence for edge-neighborhood diagrams; add as features to directed baseline. | Lower variance/higher recurrence edges enrich positives and improve calibration slope/ECE. | Label-permuted bootstraps; layer-order permutation vineyards. | medium | medium |
| N303 | Signed directed motif homology (feed-forward and feedback motif persistence) provides a mechanistic edge signal beyond pairwise scores. | Build motif-complex features (3-node signed directed motifs) and persistence depth per candidate edge; evaluate nested-model gain vs directed baseline. | Positive nested-model gain and significant motif feature coefficients across `>=2` domains. | Sign shuffle preserving degree; direction randomization preserving degree sequence. | medium | medium |
| N304 | Cross-domain similarity of persistence landscapes around genes predicts which edges transfer under disjoint splits. | Compute per-gene persistence landscapes by layer/domain; create edge-level source-target landscape similarity; evaluate transfer uplift in held-out domain splits. | Stronger transfer uplift in high-similarity strata, especially source-disjoint lung/external-lung. | Random gene-neighborhood pairing; Euclidean-distance-only landscape baseline. | high | medium |
| N305 | Curvature helps only when combined with directed margin concentration, explaining prior curvature-only failures. | Compute Ollivier-Ricci or Forman curvature on kNN edges; fit interaction model `topology_score * curvature`; compare to additive model. | Positive interaction term and significant nested-model improvement in `>=2/3` domains. | Curvature on rewired graphs; shuffled interaction partner. | medium | medium |
| N306 | Regulatory edges occupy low-holonomy/low-triangle-excess loops at multiple scales. | Compute multiscale triangle excess and holonomy defect around edge endpoints (`k=8,10,14`); test incremental value over geodesic baseline. | Positive incremental AUROC in deeper layers with reproducible split behavior. | Endpoint swap within distance bins; loop-membership permutation. | medium | low |
| N307 | Layer-transition ID gradients (across adjacent layers) are predictive even though same-layer endpoint ID jump failed. | Estimate local ID per layer (TWO-NN + MLE); derive edge features from `ΔID` and consistency across transitions (`0->3`, `3->7`, `7->11`); broad-screen against geodesic baseline. | Positive delta in `>=4/6` domain-splits, concentrated in late transitions. | Layer-order permutation per gene; endpoint swap within degree bins. | medium | low |
| N308 | Local tangent-space alignment (principal-angle similarity) between regulator and target neighborhoods marks true edges. | For each node/layer, fit local PCA tangent basis; compute edge principal-angle metrics; test incremental value over distance/topology baselines. | Positive incremental AUROC with improved calibration vs distance-only. | Neighborhood permutation preserving degree and density. | medium | medium |
| N309 | Cross-model transfer should be judged by null-gap (`observed delta - random-map quantile`), not raw delta. | Refit alignment (Procrustes/CCA variants) while optimizing null-gap objective; report per-row null-gap and aggregated significance. | Null-gap > 0 and `p<0.05` aggregates in `>=2/3` domains. | Random-map alignment, signature-destroy permutation, domain-pair shuffle. | high | medium |
| N310 | Biologically anchored contrastive alignment (TF-target and hub anchors) will unlock genuine cross-model structure transfer. | Build anchors from TRRUST/DoRothEA/STRING neighborhoods; train contrastive mapping between scGPT/Geneformer topology signatures; evaluate held-out transfer and null-gap. | Clear transfer gain in anchor-consistent strata and improved global significance vs raw mapping. | Anchor-label shuffle; degree-matched fake-anchor controls. | high | high |
| N311 | Cycle-consistent multi-domain shared topology space will suppress spurious random-map wins and improve robust transfer. | Learn shared latent space with cycle-consistency losses across immune/lung/external-lung signatures; compare to one-way alignment. | Reduced split variance and stronger domain-level transfer significance. | Remove cycle loss ablation; single-domain-only mapping control. | medium | high |
| N312 | Cell-ontology proximity modulates when topological edge scores are biologically plausible and predictive. | Map genes to ontology-linked programs; add ontology-distance interactions with topological scores; evaluate calibration and AUROC stratified by ontology proximity. | Positive interaction; largest uplift in ontology-proximal strata and failure slices. | Ontology-label shuffle; random program assignments matched by size. | high | medium |
| N313 | Persistence of regulatory motifs across layers encodes mechanistic control circuits enriched in curated networks. | Track feed-forward/feedback motif membership persistence per edge/module across layers; test enrichment and predictive gain. | Significant enrichment against TRRUST/STRING and measurable incremental classification gain. | Motif-label permutation preserving degree/sign counts. | medium | medium |
| N314 | Non-normal dynamics proxies of local layer transitions reveal regulator control points missed by static geometry. | Estimate local transition operators between adjacent layers; compute commutator norm/pseudospectral proxy per node and edge interactions with topology scores. | Positive interaction in domains where topology discrimination does not transfer cleanly. | Layer-order permutation; neighborhood reassignment controls. | medium | high |

## Top 3 for Immediate Execution
| Role | Candidate | Why now | Minimal success gate |
|---|---|---|---|
| High-probability discovery | N301 (support-margin multiparameter persistence) | Directly targets current localized failures while staying close to the strongest active branch. | Both source-disjoint failure slices become non-negative and at least `4/6` domain-splits keep positive mean incremental AUROC. |
| High-risk/high-reward | N310 (biologically anchored contrastive cross-model alignment) | Highest upside for unlocking a currently blocked transfer direction with mechanistic anchors. | Domain-level null-gap significance (`p<0.05`) in at least `2/3` domains plus positive mean transfer delta. |
| Cheap broad-screen | N307 (layer-transition ID gradient screen) | Fast to implement using existing ID estimators and immediately tests whether ID signal exists in transitions, not endpoints. | Positive mean incremental AUROC in `>=4/6` domain-splits with at least one Fisher-significant aggregate. |
