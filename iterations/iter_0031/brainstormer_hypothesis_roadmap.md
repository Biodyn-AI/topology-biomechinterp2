# Brainstormer Hypothesis Roadmap - iter_0031

## Retire / Deprioritize
| Direction | Status | Why | Re-entry condition |
|---|---|---|---|
| Global cross-model mapping/transfer endpoints (`H59/H62/H65/H68/H71/H74/H77`) | `retire_now` | Repeated major redesigns still produce null-gap collapse or utility-negative deltas. | Re-open only with biology-stratified, edge-free invariants (pathway/module state alignment). |
| Rewiring-survival stronger-null branch (`iter_0005`-`iter_0008`) | `retire_now` | Uniformly non-supportive under multiple calibration variants. | Re-open only if kNN graph-construction policy is materially changed. |
| Standalone intrinsic-dimension utility lift (`H42/H54/H60/H63/H66`) | `retire_now` | Consecutive negatives without stable rescue slice. | Use ID only as interaction terms with geometry/topology. |
| Two-axis/rank-surface persistence utility forms (`H64/H67`) | `retire_now` | Back-to-back all-split negative outcomes. | Re-open as topology localization/stability analysis, not direct lift. |
| Path-homology utility-transfer endpoint (`H53/H56`) | `retire_now` | Endpoint failed despite directional discrimination. | Re-open only with local mechanistic endpoint. |
| Support-interaction recipes in current form (`H73/H76`) | `rescue_once_with_major_change` | Core triangle-defect signal survives but interaction robustness fails, especially immune/source. | One targeted TF-module-conditioned redesign; retire if immune/source remains null-gap negative. |
| Curvature-as-score family (`H23/H61/H75`) | `rescue_once_with_major_change` | Static and acceleration versions are weak/negative. | One Ricci-flow or transport-consistency redesign. |
| Detour-elasticity current implementation (`H78`) | `rescue_once_with_major_change` | Directional positives exist but all domain-splits fail strict null-gap. | One explicit neighbor-dropout perturbation redesign. |

## New Hypothesis Portfolio
| ID | Track | Hypothesis (one sentence) | Concrete test design | Expected signal if true | Null/control | Value | Cost |
|---|---|---|---|---|---|---|---|
| N385 | topology / filtration | True regulatory edges have consistent local persistence signatures across geodesic-, support-, and ontology-weighted filtrations. | Build edge-centered ego complexes at layers `{7,11}` and compute persistence images under the three filtrations; add cross-filtration agreement features over `H70` baseline. | Positive incremental AUROC with strongest gain in immune/source. | Filtration-label permutation, degree-geodesic matched edge sampling, label shuffle. | high | medium |
| N386 | topology stability | True edges maintain H1 lifetime under landmark dropout while matched negatives collapse quickly. | For each edge neighborhood, run witness-complex subsampling at dropout rates `{0.2,0.4,0.6}` and use lifetime-retention slope features. | Flatter retention curves for positives and improved precision@k. | Landmark resampling null matched on neighborhood size, plus label shuffle. | medium | medium |
| N387 | topology (split-zigzag) | Edges that are topologically persistent in both source- and target-disjoint complexes are biologically real. | Construct split-specific complexes and compute split-zigzag overlap persistence score per edge across seeds/layers `{7,11}`. | Positive mean delta with null-survival in at least `3/6` domain-splits. | Split-label swap, split-specific rewiring within geodesic bins. | high | high |
| N388 | manifold geometry (curvature) | True edges lie in neighborhoods with stable Ricci-flow trajectories across depth rather than volatile curvature changes. | Compute Ollivier/Forman curvature on layer graphs `{0,3,7,11}` and derive trajectory features (slope, sign-change count, minima). | At least one domain-split with positive null-gap and improved global ranking. | Degree-preserving rewiring and layer-order permutation. | high | medium |
| N389 | manifold geometry (geodesics) | Positive edges have lower detour inflation under targeted neighbor dropout than matched negatives. | Redesign `H78`: drop top local-betweenness neighbors at `{10%,20%,30%}` and recompute detour elasticity features across kNN scales. | Non-negative global delta, positive means in `>=4/6` domain-splits, and at least one positive null-gap split. | Matched random-node dropout, endpoint swap within bins, label shuffle. | medium | low |
| N390 | manifold geometry (local linearity) | Parallel-transport inconsistency between endpoint tangent spaces is lower for true regulatory edges. | Compute local PCA tangent bases at endpoints over multiple neighborhood radii and derive principal-angle transport inconsistency metrics. | Positive incremental utility, strongest in immune/source and lung/source failure slices. | Neighborhood permutation preserving local density and degree bins. | medium | medium |
| N391 | intrinsic dimension interaction | `H70` lift is concentrated in intermediate-ID and high-local-linearity bands rather than uniformly across manifold regimes. | Fit stratified interaction model `H70_score x ID_band x linearity_band` across domains/splits/layers. | Significant positive interaction coefficients and targeted recovery in immune/source. | Shuffle ID and linearity within geodesic bins; layer-label permutation. | high | low |
| N392 | cross-model alignment (edge-free) | Cross-model structure is recoverable at pathway-centroid level even when gene-level mapping fails. | Build TRRUST/GO pathway centroids per model/layer and evaluate held-out-domain Procrustes/CKA/retrieval. | Immune and at least one non-immune domain exceed null q95 on centroid concordance. | Pathway-membership permutation preserving pathway sizes. | high | medium |
| N393 | cross-model mechanistic motifs | Motif-state distribution over layer trajectories is conserved across models within the same biological strata. | Derive motif-state sequences per edge in each model and compare stratum-level state distributions with EMD/JSD. | Lower divergence than shuffled maps with consistent cross-domain pattern. | State relabel shuffle, stratum shuffle, degree-matched randomization. | medium | low |
| N394 | cross-model triplet invariants | TF-anchored triplet margins (target+ vs target-) transfer across models even if direct edge transfer fails. | Build TRRUST/Dorothea triplets and measure cross-model margin correlation and rank agreement across domains/layers `{7,11}`. | Positive cross-model margin concordance and immune null-gap > 0. | Triplet target swaps within TF and random triplet controls. | high | medium |
| N395 | biological anchoring (targeted rescue) | Immune/source failure is driven by TF-module heterogeneity and can be rescued with module-conditional support calibration. | Partition immune/source edges by TRRUST TF module + STRING local density, then learn module-conditioned support interaction over `H70` scores. | Immune/source `delta_AUROC > 0` and interaction null-gap turns positive in at least one immune split. | Module-label permutation, matched random module weights, label shuffle. | high | medium |
| N396 | biological anchoring (cell ontology) | H70 lift is reliably larger for cell-ontology-consistent endpoint pairs than inconsistent pairs. | Add ontology semantic similarity tier and test monotonic interaction with triangle-defect lift across domains/splits. | Positive monotonic trend with better calibration in high-concordance tier. | Ontology-term permutation preserving frequency and tier-size controls. | medium | low |
| N397 | algorithmic signatures | True edges are enriched for reroute-critical triangle motifs where small local ablations cause large detour jumps. | For each edge, ablate local nodes/links around shared neighborhoods and measure detour jump and triangle-defect sensitivity. | Significant enrichment of high-criticality motif class and improved top-k precision. | Degree-matched random ablations and label shuffle. | medium | medium |
| N398 | mechanistic motifs | A compact finite-state automaton of layerwise edge trajectories contains states specifically enriched for regulatory positives. | Discretize multi-layer feature trajectories with MDL-selected alphabet and test transition-state enrichment plus predictive lift. | At least one state/transition survives multiple-testing correction and increases AUROC. | Layer-order permutation and sequence shuffle within geodesic bins. | medium | medium |

## Top 3 for Immediate Execution
| Role | Candidate | Why now | Minimal success gate |
|---|---|---|---|
| High-probability discovery candidate | N395 | Directly targets the known failure slice (`immune/source`) while preserving the validated `H70` backbone. | Immune/source `delta_AUROC > 0`, global interaction mean `> 0`, and positive interaction null-gap in `>=2/6` domain-splits. |
| High-risk/high-reward candidate | N392 | Gives cross-model work a materially different objective (pathway-centroid geometry), avoiding repeated gene-level map failures. | Immune plus at least one non-immune domain exceed null q95 on centroid concordance metrics. |
| Cheap broad-screen candidate | N389 | Low implementation cost and strong diagnostic value for whether geodesic robustness is real or artifact-driven. | Non-negative global mean delta, positive means in `>=4/6` domain-splits, and at least one split with positive null-gap. |
