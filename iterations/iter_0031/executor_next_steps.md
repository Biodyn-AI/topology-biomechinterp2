# Executor Next Steps — iter_0031

1. Retire the current cross-model relational-rank mapping formulation (`H77`) for now; any future re-entry should require biologically stratified or motif-state endpoints, not global pairwise rank transfer.
2. Keep one low-budget follow-up for `H78` only if the method is materially changed to improve null-gap robustness (for example explicit neighbor-dropout perturbations or stronger matched endpoint controls).
3. Continue with the `H70` lineage only through higher-specificity biological anchoring designs that directly target immune/source failure slices; do not reuse the current `H76` interaction recipe unchanged.
