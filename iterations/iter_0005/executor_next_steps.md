# Executor Next Steps — iter_0005

1. Replace the current `distance_permutation` null with a topology-preserving stronger control (kNN degree-preserving rewiring or geodesic graph rewiring) and rerun the same domain/split/layer grid.
2. Expand split-robustness from 2 layers/domain to all 12 layers for at least one domain (immune first) to map where split brittleness appears.
3. Anchor positive split-robust layers biologically by extracting high-contribution genes and testing TRRUST/GO/STRING enrichment.
