# Executor Next Steps - iter_0038

1. Retire the current bridge-curvature additive utility lineage (`H95/H97`) as a primary branch: despite repeated large directional lift, stricter rewiring controls still yield `0/6` positive mean null-gap domain-splits.
2. Do not spend another immediate slot on standalone/additive intrinsic-dimension utility forms (`H98`-style); if revisited, restrict to interaction-only designs on top of already robust topology backbones (`H91/H93`).
3. Keep cross-model alignment retired in its current endpoint family (`H96/H99`); only reopen with materially new shared residual supervision or matched latent tensor views and a hard fast-fail gate.
4. Next packet should prioritize non-retired high-upside topology directions (`persistent_homology` relative/contrastive formulations and `split_robustness` confirmations) with at least one new family-level method and explicit null-gap gates.
